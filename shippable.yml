python:
    - "2.7"
    - "3.4"
language: python
before_script: mkdir -p shippable/codecoverage shippable/testresults
install: pip install --use-mirrors -r requirements.txt
script:
  - cd technical_indicators
  - python -m doctest -v ../test/test.rst
notifications:
  email:
   - jcrmatos@gmail.com
